name: vm_launcher
description: Geogram Dev VM Launcher
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  xterm: ^4.0.0
  path: ^1.9.0
  http: ^1.2.0
  window_manager: ^0.4.3
  shared_preferences: ^2.2.3
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
